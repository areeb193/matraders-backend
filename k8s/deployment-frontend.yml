# ==============================================================================
# FRONTEND DEPLOYMENT FOR KUBERNETES
# ==============================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: matraders-frontend
  namespace: matraders
  labels:
    app: matraders-frontend
    tier: frontend
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: matraders-frontend
  template:
    metadata:
      labels:
        app: matraders-frontend
        tier: frontend
    spec:
      imagePullSecrets:
      - name: dockerhub-secret
      containers:
      - name: frontend
        image: areeb193/matraders-frontend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
          name: http
        env:
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: matraders-config
              key: NODE_ENV
        - name: MONGO_URI
          valueFrom:
            configMapKeyRef:
              name: matraders-config
              key: MONGO_URI
        - name: NEXT_PUBLIC_API_URL
          value: "http://matraders-backend:3000"
        - name: PORT
          valueFrom:
            configMapKeyRef:
              name: matraders-config
              key: PORT
        - name: NEXT_TELEMETRY_DISABLED
          valueFrom:
            configMapKeyRef:
              name: matraders-config
              key: NEXT_TELEMETRY_DISABLED
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
