# Production Environment Variables
# Copy this to .env.local and fill in the values

# JWT Configuration (REQUIRED - Change this to a secure random string!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars
JWT_EXPIRY=600s

# MongoDB Configuration (REQUIRED)
MONGODB_URI=your-mongodb-connection-string-here

# Node Environment
NODE_ENV=production

# Security
NEXT_PUBLIC_API_URL=https://your-domain.com

# Rate Limiting (Optional - for production Redis integration)
# REDIS_URL=redis://localhost:6379

# Session Configuration
SESSION_COOKIE_NAME=auth_token
SESSION_MAX_AGE=600

# Email Configuration (for future email verification)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Admin Configuration (Optional - for initial admin setup)
ADMIN_EMAIL=admin@matraders.com
ADMIN_PASSWORD_HASH=change-this-to-bcrypt-hash

# Monitoring and Logging (Optional)
# SENTRY_DSN=your-sentry-dsn
# LOG_LEVEL=error

# HTTPS Enforcement (Production)
FORCE_HTTPS=true
